# Atlas Settings

Local UI data and generated configuration files are written here.

Current runtime files:

- `api-token.json`: API token saved from the web UI settings form.

This directory is intentionally git-ignored except for this README and the
placeholder `.gitkeep` file.
